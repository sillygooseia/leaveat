{
  "name": "leaveat",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "https://github.com/sillygooseia/leaveat"
  },
  "private": true,
  "description": "LeaveAt — employee scheduling tool built on the BafGo SDK",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:license": "cd license && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "install:all": "cd backend && npm install && cd ../license && npm install && cd ../frontend && npm install"
  }
}
