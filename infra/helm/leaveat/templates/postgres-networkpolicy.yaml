{{- if .Values.postgres.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "leaveat.fullname" . }}-postgres
spec:
  podSelector:
    matchLabels:
      app: {{ include "leaveat.name" . }}-postgres
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchExpressions:
              - key: app
                operator: In
                values:
                  - {{ include "leaveat.name" . }}-backend
                  - {{ include "leaveat.licenseName" . }}
                  - {{ include "leaveat.name" . }}-postgres-backup
      ports:
        - protocol: TCP
          port: 5432
{{- end }}